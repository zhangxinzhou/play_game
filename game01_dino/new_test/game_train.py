# 演化主流程

def model_evolution():
    # 环境容量-一代模型最大数量
    env_capacity = 100
    # 训练最大世代
    generation_max = 1000

    for generation_num in range(generation_max):
        # 查询全部待训练模型,如果没有则生成一个初始模型
        model_generation_list = []
        if model_generation_list is None or len(model_generation_list) == 0:
            # 生成一个初始模型
            init_fully_connected_layer = []
            pass

        for model_generation in model_generation_list:
            pass
            # 训练模型,并产生随机变异体
            # 繁衍权-下一代模型总数小于环境容量
            reproductive_rights = True
            model_generation

    pass
